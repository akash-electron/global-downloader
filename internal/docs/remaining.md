Next Things You Should Add (Important)

Your real downloader should support:

queue system
progress tracking
job id
multiple formats
audio extraction
cloud storage

Example future API:

POST /download
GET /job/{id}
GET /file/{id}

If you want, I can also show you how to turn this into a REAL production downloader like:

snapinsta
savefrom
y2mate

with:

async workers

job queue

video streaming

10k downloads/min architecture

It's actually a very cool system design.